# Optional: require a bearer token for the web UI/API
# TABYBOT_WEB_TOKEN=

# Optional: change the web UI port (default 8999)
# TABYBOT_PORT=8999

# Optional: bind a host folder into the container at /workspace (see README)
# HOST_WORKSPACE=/absolute/path/to/your/project
